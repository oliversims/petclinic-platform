# =============================================================================
# k8s-reference/base/visits-service/service.yaml
# Purpose: ClusterIP on 8082, reachable in-cluster as http://visits-service:8082.
# =============================================================================

# =============================================================================
# Kubernetes API and resource type
# Purpose: Defines this Kubernetes resource as a Service.
# =============================================================================
apiVersion: v1
kind: Service

# =============================================================================
# Service metadata
# Purpose: Defines the Service name, namespace, and identifying labels.
# The name is the DNS name other Pods use: http://visits-service:8082.
# It must match metadata.name on the Deployment and ServiceAccount.
# =============================================================================
metadata:
  name: visits-service
  namespace: petclinic-dev
  labels:
    app.kubernetes.io/name: visits-service
    app.kubernetes.io/part-of: petclinic
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: service

# =============================================================================
# Service specification
# Purpose: Defines how traffic reaches the visits-service Pods.
# =============================================================================
spec:

  # =============================================================================
  # Service type
  # Purpose: ClusterIP only; not reachable from the internet.
  # External traffic enters through the api-gateway Ingress / ALB instead.
  # =============================================================================
  type: ClusterIP

  # =============================================================================
  # Pod selector
  # Purpose: Identifies the Pods that receive traffic.
  # Must match the Deployment selector and Pod template labels.
  # =============================================================================
  selector:
    app.kubernetes.io/name: visits-service

  # =============================================================================
  # Service ports
  # Purpose: Forwards Service port 8082 to containerPort 8082.
  # That targetPort must match the Deployment containerPort 8082.
  # Other services reach this name and port via Config Server / Discovery.
  # =============================================================================
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
